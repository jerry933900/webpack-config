// 引入 其他的js模块

// 只是引入 没有保存到变量 只是单纯的导入
// import './tools';

// 这种写法 导入的模块 需要再次使用
import tools from './tools';
import './base.css'
import './index.css'

document.body.style.backgroundColor = 'skyblue';
